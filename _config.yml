collections:
  projects:
    output: true
    permalink: /:collection/:name
  jobs:


defaults:
  - scope:
      path: ""
      type: "projects"
    values:
      layout: "project"
  - scope:
      path: ""
    values:
      layout: "default"
