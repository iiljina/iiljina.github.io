collections:
  events:
    output: true
    permalink: /:collection/:name

  proposals:
    output: true
    permalink: /:collection/:name

  testimonials:

  jobs:

  hobbies:

  summaries:

defaults:
  - scope:
      path: ""
      type: "projects"
    values:
      layout: "project"
  - scope:
      path: ""
    values:
      layout: "default"
